{
  "name": "@noether/sidecar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TypeScript client for the Noether decision sidecar API",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test test/*.test.mjs"
  },
  "exports": {
    ".": "./dist/index.js"
  },
  "types": "./dist/index.d.ts",
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
