# Dashboard visual acceptance — 2026-05-17

## Scope

Final visual review for:

- the served live dashboard at `/dashboard`
- the static export dashboard generated by `noet report dashboard`

## Environment

- served instance: `noet serve --bind 127.0.0.1:4051 --db-path /tmp/noether-visual/live.sqlite`
- browser capture: Playwright CLI using the local Chrome channel
- source ledger for populated captures: `/tmp/noether-visual/live.sqlite`

## Captured evidence

### Live dashboard

- empty state screenshot: `/tmp/noether-visual/live-empty.png`
- populated state screenshot: `/tmp/noether-visual/live-populated.png`

Observed:

- the live dashboard is visually distinct from the static export dashboard
- empty state clearly renders:
  - no decisions yet
  - no finalized usage yet
  - no trace/observation evidence yet
- populated state clearly renders:
  - trace picker with multiple traces
  - summary cards for spend/tokens/decision mix/evidence
  - policy decisions section
  - spend and adoption section
  - run evidence section

### Static export dashboard

- empty state screenshot: `/tmp/noether-visual/export-empty.png`
- populated state screenshot: `/tmp/noether-visual/export-populated.png`

Observed:

- export dashboard remains a self-contained artifact surface
- populated export dashboard preserves the richer story-first artifact layout
- empty export dashboard remains coherent and does not collapse into broken or blank sections

## Refresh verification

Automated verification covers the live refresh transport:

- `server::tests::report_update_stream_emits_after_authorize_finalize_and_event`

Observed behavior:

- authorize, finalize, and event ingest each emit SSE invalidation messages
- the served live dashboard JS subscribes to `/v1/reports/updates`
- the live dashboard re-fetches `/v1/reports/dashboard-data` on update notifications

## Acceptance checklist

- [x] `noet serve` exposes real reporting HTTP endpoints
- [x] `noet serve` serves a real live dashboard UI
- [x] the live UI is backed by reporting API data, not CLI HTML reuse
- [x] the static export dashboard remains a separate product surface
- [x] live and export dashboards both have coherent empty states
- [x] live and export dashboards both have coherent populated states
- [x] live refresh transport is present and verified
